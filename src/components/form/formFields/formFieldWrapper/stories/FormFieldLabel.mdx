import { Meta, Controls, Canvas } from '@storybook/blocks';
import * as FormFieldLabelStories from './FormFieldLabel.stories';

# FormFieldLabel: FormField Subcomponent

The FormFieldLabel component is not typically used on its own. It renders as a subcomponent of the FormField component.

<Meta of={ FormFieldLabelStories } />

<Canvas of={FormFieldLabelStories.LabelSubComponent} />

<Controls />
